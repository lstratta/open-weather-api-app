pipeline {

    agent any

    environment {
        API_ENV = credentials('OpenWeatherAPI')
        TEST_ENV = credentials('jenkins-test-cred')
    }

    stages {
        stage('Build') {
            steps {
                echo "Building"
                sh "echo ${TEST_ENV}"
                // sh 'npm i'
            }
        }

        stage('Test') {
            steps {
                echo "Testing"
                // sh 'npm test'
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying"
            }
        }
    }
}