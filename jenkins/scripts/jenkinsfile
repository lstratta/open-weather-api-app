pipeline {

    agent any

    tools {
        nodejs "NodeJS 16.14.0"
    }

    environment {
        API_ENV = credentials('OpenWeatherAPI')
        TEST_ENV = credentials('jenkins-test-cred')
    }

    stages {
        stage('Build') {
            steps {
                nodejs(nodeJSInstallationName: 'Node 16.x')
                echo "Building"

                sh 'npm i'
            }
        }

        stage('Test') {
            steps {
                echo "Testing"
                // sh 'npm test'
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying"
            }
        }
    }
}