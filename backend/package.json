{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test mocha --timeout 10000 --exit",
    "start": "NODE_ENV=prod nodemon server/server.js",
    "dev": "NODE_ENV=dev nodemon server/server.js",

    "testwin": "SET NODE_ENV=test && mocha --timeout 10000 --exit",
    "startwin": "SET NODE_ENV=development && nodemon server/server.js"
  },
  "author": "Luke Stratta",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.0",
    "chai": "^4.3.6",
    "chai-http": "^4.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.2",
    "mocha": "^9.2.0",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.15"
  }
}
